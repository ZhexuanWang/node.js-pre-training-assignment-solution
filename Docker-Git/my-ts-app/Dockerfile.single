FROM node:24

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY tsconfig.json ./
COPY src/ ./src/

# Run tsc directly with explicit config
RUN ./node_modules/.bin/tsc --project tsconfig.json

EXPOSE 3000

CMD ["node", "dist/index.js"]
